import { Injectable } from '@angular/core';

/**
 * Let assets be accumulated then operated on.
 * 
 * Assets can be exchanged with one another
 * This service faciliates trading between different assets e.g. switching from cash to stocks
 */
@Injectable({
  providedIn: 'root'
})
export class AssetTradingService {
  constructor() {
    

  }
}
