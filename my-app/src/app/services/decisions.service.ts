import { Injectable } from '@angular/core';

/**
 * Develop a decision tree where based on the current snapshot, what to do next
 * - Make specific kinds of trades
 * - Can invest in school to increase chances of a higher paying job / wages
 * - Can change when kid will arrive
 * - 
 */


@Injectable({
  providedIn: 'root'
})
export class DecisionsService {

  constructor() { }
}
